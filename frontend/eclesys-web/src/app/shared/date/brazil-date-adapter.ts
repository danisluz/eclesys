// Legacy file kept temporarily to avoid import breakages during migration.
// Angular Material date adapter is no longer used.
export class BrazilDateAdapter {
  readonly migrated = true;
}

export const BRAZIL_DATE_FORMATS = {};
